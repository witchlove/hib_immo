<div class="row">
    <div class="col-md-12">
        {{ projectName }}
    </div>
</div>
<div>
    <div class="col-md-12">
        {{{ projectDescription }}}
        <ul>
            {{#each prop in projectProperties}}
                <li>{{prop.description}}</li>
            {{/each}}
        </ul>
    </div>

</div>